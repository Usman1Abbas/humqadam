fastapi==0.115.6
uvicorn[standard]==0.34.0
python-dotenv==1.0.1
openai==1.59.6
edge-tts==7.2.8
pydantic==2.10.4

# Optional: enable semantic RAG retrieval (pulls in torch, ~2GB).
# Not required for the MVP — the curated KB is injected as grounding context.
# sentence-transformers==3.3.1
